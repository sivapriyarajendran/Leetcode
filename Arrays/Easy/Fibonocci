class Solution {
    public int fib(int N) {
        int a=1;
        int b=1;
        int c=0;
        if(N==0)return 0;
        if(N<=2)return b;
        for(int i=3;i<=N;i++){
            c=a+b;
            a=b;
            b=c;
        }
        return c;
    }
}
